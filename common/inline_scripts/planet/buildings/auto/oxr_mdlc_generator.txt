base_buildtime = @[ $TIER$ * 180 ]
category = resource
resources = {
	category = oxr_mdlc_world_machines_generator_buildings
	cost = {
		minerals = @[ $TIER$ * 200 ]   # dont add the extra 200 any more
		# $RS_RARE$ = @[ $TIER$ * 15 ]
	}
	produces = {
		energy = @[ $TIER$ * oxr_mdlc_wm_base_technician_output ]
	}
	upkeep = {
		physics_research = @[ ( $TIER$ * 3 ) - 3 ]  # 3 upkeep at tier 2, 12 upkeep at tier 5
		# mineral upkeep
		minerals = @[ $TIER$ * 2.5 ]
	}
	# generic energy upkeep
	upkeep = {
		energy = @[ $TIER$ + 1 ]
	}

	# BIOFUEL CODE
	upkeep = {
		trigger = {
			NOT = { has_active_building = oxr_mdlc_world_machines_biofuel_refit_1 }
		}
		# mineral tax on energy buildings is the only logical option available
		# unless the upkeep is simply energy
		$RS_UPKEEP$ = @[ $TIER$ * 2 ]
	}
	upkeep = {
		trigger = {
			has_active_building = oxr_mdlc_world_machines_biofuel_refit_1
		}
		food = @[ $TIER$ * 4 ]
	}
}
# desc
inline_script = planet/buildings/auto/oxr_auto_biofuel

building_sets = {
	oxr_mdlc_building_set_wm_all
	oxr_mdlc_building_set_wm_raw_resource
	oxr_mdlc_building_set_wm_energy
}

# EOF
