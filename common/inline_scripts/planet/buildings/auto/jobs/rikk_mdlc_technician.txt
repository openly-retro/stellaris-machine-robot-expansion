produces = {
	trigger = { exists = owner owner = { NOT = { has_tradition = tr_nanotech_1 } } }
	energy = @[ $JOBS$ * oxr_mdlc_wm_base_technician_output ]
}
upkeep = {
	# base job upkeep
	energy = $JOBS$
}

# EOF
