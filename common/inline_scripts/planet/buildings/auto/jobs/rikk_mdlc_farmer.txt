produces = {
	trigger = { exists = owner owner = { NOT = { has_tradition = tr_nanotech_1 } } }
	food = @[ $JOBS$ * oxr_mdlc_wm_base_farmer_output ]
}
upkeep = {
	# base job upkeep
	energy = $JOBS$
}
produces = {
	trigger = { exists = owner owner = { has_tradition = tr_nanotech_1 } }
	food = @[ $JOBS$ * oxr_mdlc_wm_base_farmer_output - ($JOBS$ * 2) ]
}
produces = {
	trigger = { exists = owner owner = { has_tradition = tr_nanotech_1 } }
	nanites = @[ $JOBS$ * oxr_mdlc_wm_base_drone_nanite_output ]
}

#EOF
