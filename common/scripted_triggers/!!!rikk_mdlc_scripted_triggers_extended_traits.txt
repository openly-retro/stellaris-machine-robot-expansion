# TRAITS

can_add_overclocked_traits = {
	hidden_trigger = { exists = from }
	from = { has_origin = rikk_mdlc_origin_overclocked }
}

# Imported from the last known version of Extended Machines by mute
has_mute_extended_machines = {
	always = no
}
